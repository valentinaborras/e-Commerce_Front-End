import ScrollToTop from "react-scroll-to-top";

function ScrollToTopBtn() {
  return (
    <div>
      <ScrollToTop
        smooth
        style={{
          borderRadius: 50,
        }}
      />
    </div>
  );
}

export default ScrollToTopBtn;
